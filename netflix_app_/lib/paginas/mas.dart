import 'package:flutter/material.dart';

class MasPage extends StatelessWidget {
  const MasPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pagina Mas'),
    );
  }
}
