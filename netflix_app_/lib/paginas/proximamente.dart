import 'package:flutter/material.dart';

class ProximamentePage extends StatelessWidget {
  const ProximamentePage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pagina Proximamente'),
    );
  }
}
